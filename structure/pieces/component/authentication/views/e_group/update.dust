{>"layout_MODULE_NAME"/}
{<header}
    <h1>{@__ key="module.home"/}<small>{@__ key="module.module"/}</small></h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-home"></i>{@__ key="module.home"/}</a></li>
        <li class="active">{@__ key="module.home"/}</li>
    </ol>
{/header}
{<content}
    <legend>{@__ key="operation.update"/} {@__ key="entity.e_group.name_entity"/}</legend>
    <div class="row">
        <div class="col-xs-12 connectedSortable">
            <form action="/group/update" method="post">
                <div class="box-body">
                    <div class="form-group">
                        <label for="id">{@__ key="entity.e_group.id_entity"/} {@__ key="entity.e_group.name_entity"/}</label>
                        <input name="id" type="hidden" placeholder="{e_group.id_entity}" value="{e_group.id}" class="form-control input"/>
                        <input name="id_display" type="text" placeholder="{e_group.id_entity}" value="{e_group.id}" disabled="" class="form-control input"/>
                    </div>
                    {#e_group}
                        {>"e_group/update_fields"/}
                    {/e_group}
                    <div class="form-group">
                        <input name="version" value="{e_group.version}" type="hidden" placeholder="version" class="form-control input"/>
                    </div>
                </div>
                <div class="box-footer">
                    <button type="submit" class="btn btn-primary">{@__ key="button.save"/}</button>
                </div>
                {#associationFlag}
                    <input type="hidden" name='associationFlag' value="{.}">
                {/associationFlag}
                {#associationSource}
                    <input type="hidden" name='associationSource' value="{.}">
                {/associationSource}
                {#associationForeignKey}
                    <input type="hidden" name='associationForeignKey' value="{.}">
                {/associationForeignKey}
                {#associationAlias}
                    <input type="hidden" name='associationAlias' value="{.}">
                {/associationAlias}
                {#associationUrl}
                    <input type="hidden" name='associationUrl' value="{.}">
                {/associationUrl}
            </form>
        </div>
    </div>
{/content}